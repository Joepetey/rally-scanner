[build]
builder = "RAILPACK"
buildCommand = "python -m venv /app/.venv && /app/.venv/bin/pip install --no-cache-dir ."

[deploy]
startCommand = "bash start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[deploy.volumes]]
mountPath = "/app/models"
name = "rally-models"
