# Market Rally Notification Configuration
# Copy to .env and fill in values for the backends you want.
# Leave blank to disable a backend.

# --- Telegram (recommended) ---
# 1. Message @BotFather on Telegram, /newbot, follow prompts
# 2. Copy the token below
# 3. Message your bot, then visit https://api.telegram.org/bot<TOKEN>/getUpdates
# 4. Find your chat_id in the response
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Email (SMTP) ---
# For Gmail: use an App Password (https://myaccount.google.com/apppasswords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
NOTIFY_EMAIL=

# --- Discord ---
# 1. Create a bot at https://discord.com/developers/applications
# 2. Enable MESSAGE CONTENT intent in Bot settings
# 3. Copy the bot token below
# 4. Invite bot to your server with bot + applications.commands scopes
# 5. Right-click the alerts channel → Copy Channel ID (enable Developer Mode)
DISCORD_BOT_TOKEN=
DISCORD_CHANNEL_ID=
# Optional: Discord webhook URL (fallback if bot token not set)
DISCORD_WEBHOOK_URL=

# --- Scheduler (cloud deployment) ---
# Set to 1 to run scan/retrain on a schedule inside the bot process.
# This replaces cron — use when deploying to Railway/Render/etc.
# Scan: Mon-Fri 4:30 PM ET, Retrain: Sunday 6 PM ET
ENABLE_SCHEDULER=0

# --- Webhook (Slack/custom) ---
WEBHOOK_URL=
